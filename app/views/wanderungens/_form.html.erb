<%= form_for(@wanderungen) do |f| %>
  <% if @wanderungen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wanderungen.errors.count, "error") %> prohibited this wanderungen from being saved:</h2>

      <ul>
      <% @wanderungen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :schwierigkeitsgrad %><br>
    <%= f.collection_select(:schwierigkeitsgrad_id, Schwierigkeitsgrad.all, :id, :description, :include_blank => true) %>
  </div>
  <div class="field">
    <%= f.label :zeitdauer %><br>
    <%= f.number_field :zeitdauer, :step => 0.5 %>
  </div>
   <div class="field">
    <%= f.label :moreinfo %><br>
    <%= f.text_field :moreinfo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
